"""The FastAPI web app (`design.md` §15): `app.py`, `routers/`, `templates/`.

`static/` is added later, when a local (non-CDN) asset is first needed
(issue #34's "Out of scope").
"""

from __future__ import annotations
