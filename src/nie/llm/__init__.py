"""LLM client wrapper package.

`nie.llm.client` gives every pipeline stage one resilient, config-driven
way to call the LLM. Per-stage prompt content lives under
`nie.llm.prompts` (owned by later issues), not here.
"""
