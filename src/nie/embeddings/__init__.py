"""Local embeddings package (#21).

Currently a single backend, `nie.embeddings.fastembed`. Kept as a package
(rather than a single `nie/embeddings.py` module) so a future alternate
backend has somewhere to live without a rename.
"""

from __future__ import annotations
